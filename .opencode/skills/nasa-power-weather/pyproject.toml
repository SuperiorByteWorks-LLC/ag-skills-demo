[project]
name = "nasa-power-weather"
version = "1.0.0"
description = "NASA POWER weather data downloader"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0.0",
    "requests>=2.28.0",
    "shapely>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
